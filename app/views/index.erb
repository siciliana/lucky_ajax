<div class="container">
  <h1>Simplest Possible AJAX</h1>
  <p>This contrived app will simulate a roll of a 6-sided die.</p>

  <form id="roll" method="post" action="/rolls" data-value="<%= rand(1..6) %>">
    <input type="hidden" value="<%= rand(1..6) %>" name="value"> 
    <input type="submit" value="Roll the Die">
  </form>

  <div id="die">
    <% if @roll %>
      <%= erb :_die, layout: false %> 
    <% end %>
  </div>

</div>
